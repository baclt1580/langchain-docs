---
title: "并发加载器集成"
description: "使用 LangChain Python 与并发加载器文档加载器进行集成。"
---

工作方式与通用加载器类似，但采用并发处理，适合希望优化工作流程的用户。

```python
from langchain_community.document_loaders import ConcurrentLoader
```

```python
loader = ConcurrentLoader.from_filesystem("example_data/", glob="**/*.txt")
```

```python
files = loader.load()
```

```python
len(files)
```

```text
2
```

```python

```
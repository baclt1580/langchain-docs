---
title: "Airbyte Zendesk Support（已弃用）集成"
description: "使用 LangChain Python 与 Airbyte Zendesk Support（已弃用）文档加载器集成。"
---

注意：此特定于连接器的加载器已弃用。请改用 [`AirbyteLoader`](/oss/integrations/document_loaders/airbyte)。

>[Airbyte](https://github.com/airbytehq/airbyte) 是一个数据集成平台，用于将来自 API、数据库和文件的 ELT 管道同步到数据仓库和湖仓。它拥有最大的 ELT 连接器目录，支持多种数据仓库和数据库。

此加载器将 Zendesk Support 连接器暴露为文档加载器，允许您将各种对象加载为文档。

## 安装

首先，您需要安装 `airbyte-source-zendesk-support` Python 包。

```python
pip install -qU  airbyte-source-zendesk-support
```

## 示例

查看 [Airbyte 文档页面](https://docs.airbyte.com/integrations/sources/zendesk-support/) 了解如何配置读取器的详细信息。
配置对象应遵循的 JSON 模式可在 GitHub 上找到：[https://github.com/airbytehq/airbyte/blob/master/airbyte-integrations/connectors/source-zendesk-support/source_zendesk_support/spec.json](https://github.com/airbytehq/airbyte/blob/master/airbyte-integrations/connectors/source-zendesk-support/source_zendesk_support/spec.json)。

其基本结构如下：

```python
{
  "subdomain": "<您的 Zendesk 子域名>",
  "start_date": "<开始检索记录的日期，ISO 格式，例如 2020-10-20T00:00:00Z>",
  "credentials": {
    "credentials": "api_token",
    "email": "<您的邮箱>",
    "api_token": "<您的 API 令牌>"
  }
}
```

默认情况下，所有字段都作为元数据存储在文档中，文本内容设置为空字符串。通过转换读取器返回的文档来构建文档的文本内容。

```python
from langchain_community.document_loaders.airbyte import AirbyteZendeskSupportLoader

config = {
    # 您的 zendesk-support 配置
}

loader = AirbyteZendeskSupportLoader(
    config=config, stream_name="tickets"
)  # 请查阅上面链接的文档以获取所有可用流的列表
```

现在您可以按常规方式加载文档

```python
docs = loader.load()
```

由于 `load` 返回一个列表，它会阻塞直到所有文档加载完成。为了更好地控制此过程，您也可以使用 `lazy_load` 方法，它返回一个迭代器：

```python
docs_iterator = loader.lazy_load()
```

请注意，默认情况下页面内容为空，元数据对象包含记录中的所有信息。要以不同的方式创建文档，请在创建加载器时传入一个记录处理函数：

```python
from langchain_core.documents import Document


def handle_record(record, id):
    return Document(page_content=record.data["title"], metadata=record.data)


loader = AirbyteZendeskSupportLoader(
    config=config, record_handler=handle_record, stream_name="tickets"
)
docs = loader.load()
```

## 增量加载

某些流支持增量加载，这意味着源会跟踪已同步的记录，不会再次加载它们。这对于数据量大且频繁更新的源非常有用。

要利用此功能，请存储加载器的 `last_state` 属性，并在重新创建加载器时传入。这将确保仅加载新记录。

```python
last_state = loader.last_state  # 安全存储

incremental_loader = AirbyteZendeskSupportLoader(
    config=config, stream_name="tickets", state=last_state
)

new_docs = incremental_loader.load()
```
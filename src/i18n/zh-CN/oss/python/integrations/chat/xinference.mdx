---
title: "ChatXinference 集成"
description: "使用 LangChain Python 与 ChatXinference 聊天模型集成。"
---

[Xinference](https://github.com/xorbitsai/inference) 是一个强大且多功能的库，旨在为 LLM、语音识别模型和多模态模型提供服务，甚至可以在您的笔记本电脑上运行。它支持多种与 GGML 兼容的模型，例如 chatglm、baichuan、whisper、vicuna、orca 等。

## 概述

### 集成详情

| 类 | 包 | 可序列化 | [JS 支持] | 下载量 | 版本 |
| :--- | :--- | :---: |  :---: | :---: | :---: |
| `ChatXinference`| langchain-xinference | ❌ | ✅ | ✅ | ✅ |

### 模型特性

| [工具调用](/oss/langchain/tools/) | [结构化输出](/oss/langchain/structured-output) | [图像输入](/oss/langchain/messages#multimodal) | 音频输入 | 视频输入 | [令牌级流式传输](/oss/langchain/streaming/) | 原生异步 | [令牌使用量](/oss/langchain/models#token-usage) | [对数概率](/oss/langchain/models#log-probabilities) |
| :---: |:----------------------------------------------------:| :---: |  :---: | :---: | :---: | :---: | :---: | :---: |
| ✅ |                          ✅                           | ❌ | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |

## 设置

通过 PyPI 安装 `Xinference`：

```python
pip install -qU  "xinference[all]"
```

### 本地或分布式集群部署 xinference

对于本地部署，运行 `xinference`。

要在集群中部署 Xinference，首先使用 `xinference-supervisor` 启动一个 Xinference 管理节点。您也可以使用选项 -p 指定端口，-H 指定主机。默认端口为 8080，默认主机为 0.0.0.0。

然后，在每台要运行 Xinference 的服务器上使用 `xinference-worker` 启动 Xinference 工作节点。

您可以查阅 [Xinference](https://github.com/xorbitsai/inference) 的 README 文件以获取更多信息。

### 包装器

要在 LangChain 中使用 Xinference，您需要先启动一个模型。您可以使用命令行界面（CLI）来操作：

```python
%xinference launch -n vicuna-v1.3 -f ggmlv3 -q q4_0
```

```text
模型 UID: 7167b2b0-2a04-11ee-83f0-d29396a3f064
```

系统会返回一个模型 UID 供您使用。现在您可以在 LangChain 中使用 Xinference：

## 安装

LangChain Xinference 集成位于 `langchain-xinference` 包中：

```python
pip install -qU langchain-xinference
```

请确保您使用的是最新版本的 Xinference 以支持结构化输出。

## 实例化

现在我们可以实例化我们的模型对象并生成聊天补全：

```python
from langchain_xinference.chat_models import ChatXinference

llm = ChatXinference(
    server_url="your_server_url", model_uid="7167b2b0-2a04-11ee-83f0-d29396a3f064"
)

llm.invoke(
    "Q: 在法国首都我们可以参观哪些地方？",
    config={"max_tokens": 1024},
)
```

## 调用

```python
from langchain.messages import HumanMessage, SystemMessage
from langchain_xinference.chat_models import ChatXinference

llm = ChatXinference(
    server_url="your_server_url", model_uid="7167b2b0-2a04-11ee-83f0-d29396a3f064"
)

system_message = "你是一个将英语翻译成法语的助手。请翻译用户的句子。"
human_message = "我喜欢编程。"

llm.invoke([HumanMessage(content=human_message), SystemMessage(content=system_message)])
```

---

## API 参考

有关 `ChatXinference` 所有功能和配置的详细文档，请参阅 API 参考：[github.com/TheSongg/langchain-xinference](https://github.com/TheSongg/langchain-xinference)
---
title: "Lakefs 集成"
description: "使用 LangChain Python 与 Lakefs 集成。"
---

>[lakeFS](https://docs.lakefs.io/) 提供可扩展的版本控制
> 在数据湖上，并使用类 Git 的语义来创建和访问这些版本。

## 安装和设置

获取 `ENDPOINT`、`LAKEFS_ACCESS_KEY` 和 `LAKEFS_SECRET_KEY`。
您可以在 [lakeFS 快速入门指南](https://docs.lakefs.io/quickstart/launch.html) 中找到安装说明。


## 文档加载器

查看 [使用示例](/oss/integrations/document_loaders/lakefs)。

```python
from langchain_community.document_loaders import LakeFSLoader
```
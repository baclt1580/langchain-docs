---
title: "智谱AI集成"
description: "使用LangChain JavaScript集成智谱AI的嵌入模型。"
---

`ZhipuAIEmbeddings` 类利用智谱AI API为给定文本生成嵌入向量。

## 设置

你需要注册获取智谱AI API密钥，并将其设置为名为 `ZHIPUAI_API_KEY` 的环境变量。

https://open.bigmodel.cn

然后，你需要安装 [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) 包：

<Tip>
关于安装LangChain包的通用说明，请参阅[此部分](/oss/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core jsonwebtoken
```

## 使用示例

```typescript
import { ZhipuAIEmbeddings } from "@langchain/community/embeddings/zhipuai";

const model = new ZhipuAIEmbeddings({});
const res = await model.embedQuery(
  "一家生产彩色袜子的公司，起什么名字比较好？"
);
console.log({ res });
```

## 相关链接

- 嵌入模型[概念指南](/oss/integrations/embeddings)
- 嵌入模型[操作指南](/oss/integrations/embeddings)
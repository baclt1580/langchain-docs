---
title: 配置运行输入输出预览
sidebarTitle: 配置运行预览
description: 通过为特定追踪类型配置自定义预览路径，自定义运行表中输入和输出列显示的内容。
---

默认情况下，LangSmith 使用启发式方法来确定在**运行**表的**输入**和**输出**列中显示什么内容。但是，您可以通过为特定追踪类型配置自定义预览路径，精确自定义这些列中显示的内容。

这在以下情况下特别有用：

- 您的追踪具有深层嵌套结构。
- 您希望聚焦于数据中的特定字段。
- 默认启发式方法未能显示与您的用例最相关的信息。

## 在 UI 中配置预览格式

### 访问预览设置

1. 导航到项目中的某个追踪。
1. 选择**运行**选项卡。
1. 在运行表顶部找到 <Icon icon="adjustments-horizontal" /> **格式**按钮。
1. 在**配置输入和输出预览**侧边窗口中，从下拉菜单中选择一个追踪名称。

<img
className="block dark:hidden"
src="/langsmith/images/tracing-table.png"
alt="运行表顶部显示格式按钮，用于配置输入和输出预览"
/>

<img
className="hidden dark:block"
src="/langsmith/images/tracing-table-dark.png"
alt="运行表顶部显示格式按钮，用于配置输入和输出预览"
/>

当您选择一个追踪名称时，LangSmith 会加载一个成功的追踪示例，并将其结构渲染为一棵可展开的树。树中的每个节点代表数据中的一个字段，显示：

- 字段名称（例如，messages、output、metadata）。
- 数组索引（例如，[0]、[1]、[-1] 表示最后一项）。
- 数组的项目计数（例如，(3) 表示 3 个项目）。
- 字符串和数字的预览值以内联方式显示。

<img
className="block dark:hidden"
src="/langsmith/images/configure-preview.png"
alt="配置输入和输出预览侧边面板，显示追踪数据结构的树状视图"
/>

<img
className="hidden dark:block"
src="/langsmith/images/configure-preview-dark.png"
alt="配置输入和输出预览侧边面板，显示追踪数据结构的树状视图"
/>

### 设置路径

1. 选择**输入**或**输出**选项卡。然后，您可以选择以下任一方式：
    - 使用下拉菜单直接从输入数据中指定应显示在预览中的路径。
    - 使用示例追踪数据结构的交互式树状视图，您可以浏览并选择要显示的确切字段。

    要选择一个字段：

    1. 通过点击箭头图标（▶）展开或折叠嵌套的对象和数组来浏览树。
    1. 点击要显示在预览中的字段旁边的复选框。选定的路径会出现在树前面的文本输入框中。

    当您选择一个复选框时，路径会自动使用正确的语法构建（例如，messages[-1].content）。

| 方法 | 最适合 | 示例 |
| --- | --- | --- |
| 树状选择 | 探索不熟悉的数据结构，查看示例值 | 点击浏览：messages → [-1] → content |
| 手动输入 | 当您确切知道想要什么时，对于深层路径更快 | 输入：output.data.results[0].answer |

包含超过 3 个项目的数组会自动折叠，以防止视图过于复杂：

```
☐ messages (15)
  ☐ [0]
  ☐ [1]
  ... (点击展开全部 15 个项目)
```

点击 **...** 按钮可以展开并查看所有数组项目。

## 示例

例如，您的追踪输入如下：

```
{
  "messages": [
    {"role": "system", "content": "You are a helpful assistant"},
    {"role": "user", "content": "What is the weather today?"}
  ],
  "metadata": {
    "user_id": "user123",
    "session_id": "sess456"
  }
}
```

要显示用户的问题：

1. 展开 **messages** 节点（显示数组项目）。
1. 展开 `[1]`（第二条消息，即用户消息）。
1. 点击 **content** 旁边的复选框。
1. 输入框显示：`messages[1].content`。

或者，使用负索引表示最后一条消息：

1. 展开 **messages**。
1. 展开 `[-1]`。
1. 点击 **content**。
1. 结果：`messages[-1].content`（始终显示最后一条消息）。

<Note>
如果在树中看到 `"No paths available"`：

- 请确保在过去 7 天内至少有一个具有所选追踪名称的成功追踪。
- 该追踪必须在您正在配置的输入/输出字段中包含数据。
- 如果需要，请尝试发送一个测试追踪。
</Note>

## 后续步骤

- 了解更多关于[查看和筛选追踪](/langsmith/filter-traces-in-application)的信息。
- 探索[自定义输出渲染](/langsmith/custom-output-rendering)以进行高级可视化。
- 设置[元数据和标签](/langsmith/add-metadata-tags)来组织您的追踪。

---

<div className="source-links">
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/i18n\zh-CN\langsmith\configure-input-output-preview.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
</div>

---
title: "Prem AI 集成"
description: "使用 LangChain JavaScript 与 Prem AI 嵌入模型进行集成。"
---

`PremEmbeddings` 类使用 Prem AI API 为给定文本生成嵌入向量。

## 设置

要使用 Prem API，您需要[注册 Prem 账户并创建 API 密钥](https://studio.premai.io/sign-up)。

然后安装 [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) 包：

<Tip>
有关安装 LangChain 包的通用说明，请参阅[此部分](/oss/javascript/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```

## 使用方式

```typescript
import { PremEmbeddings } from "@langchain/community/embeddings/premai";

const embeddings = new PremEmbeddings({
  // 在 Node.js 中默认为 process.env.PREM_API_KEY
  apiKey: "YOUR-API-KEY",
  // 在 Node.js 中默认为 process.env.PREM_PROJECT_ID
  project_id: "YOUR-PROJECT_ID",
  model: "@cf/baai/bge-small-en-v1.5", // 用于生成嵌入向量的模型
});

const res = await embeddings.embedQuery(
  "一家生产彩色袜子的公司，起什么名字比较好？"
);
console.log({ res });
```

## 相关链接

- 嵌入模型[概念指南](/oss/javascript/integrations/embeddings)
- 嵌入模型[操作指南](/oss/javascript/integrations/embeddings)

---

<div className="source-links">
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/i18n\zh-CN\oss\javascript\integrations\embeddings\premai.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
</div>

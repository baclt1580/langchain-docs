---
title: "Daytona"
sidebarTitle: "Daytona"
description: "使用 Daytona 沙盒后端与 deepagents 配合，实现具有快速冷启动的隔离代码执行环境"
---

[Daytona](https://daytona.io) 提供快速启动的沙盒环境，支持多种语言。最适合 TypeScript 和 Python 开发。

## 设置

<CodeGroup>
    ```bash npm
    npm install @langchain/daytona
    ```

    ```bash yarn
    yarn add @langchain/daytona
    ```

    ```bash pnpm
    pnpm add @langchain/daytona
    ```
</CodeGroup>

### 认证

从 [app.daytona.io](https://app.daytona.io) 获取您的 API 密钥。

```bash
export DAYTONA_API_KEY=your_api_key
```

或者直接传递凭据：

```typescript
const sandbox = await DaytonaSandbox.create({
  auth: { apiKey: "your-api-key-here" },
});
```

## 与 deepagents 配合使用

```typescript
import { createDeepAgent } from "deepagents";
import { ChatAnthropic } from "@langchain/anthropic";
import { DaytonaSandbox } from "@langchain/daytona";

const sandbox = await DaytonaSandbox.create({
  language: "typescript",
  timeout: 300,
});

try {
  const agent = createDeepAgent({
    model: new ChatAnthropic({ model: "claude-sonnet-4-20250514" }),
    systemPrompt: "您是一个拥有沙盒访问权限的编码助手。",
    backend: sandbox,
  });

  const result = await agent.invoke({
    messages: [{ role: "user", content: "创建一个 hello world TypeScript 应用并运行它" }],
  });
} finally {
  await sandbox.close();
}
```


## 独立使用

```typescript
import { DaytonaSandbox } from "@langchain/daytona";

const sandbox = await DaytonaSandbox.create({
  language: "typescript",
  timeout: 300,
});

const result = await sandbox.execute("npm --version");
console.log(result.output);

await sandbox.close();
```


## 配置

| 选项 | 类型 | 默认值 | 描述 |
|--------|------|---------|-------------|
| `language` | `string`  | `"typescript"` | 主要语言运行时。选项：`"typescript" \| "python" \| "javascript"` |
| `timeout` | `number` | `300` | 命令超时时间（秒） |
| `target` | `string` | `"us"` | 区域。选项：`"us" \| "eu"`  |
| `image` | `string` | - | 自定义 Docker 镜像（例如：`"node:20"`, `"python:3.12"`） |
| `snapshot` | `string` | - | 快照名称（不能与 `image` 同时使用） |
| `resources` | `object` | - | CPU、内存、磁盘分配（需要 `image`） |
| `autoStopInterval` | `number` | `15` | 空闲 N 分钟后自动停止（0 表示禁用） |
| `labels` | `Record<string, string>` | - | 用于组织沙盒的自定义标签 |
| `initialFiles` | `Record<string, string>` | - | 启动时创建的文件 |

## 自定义资源

要自定义 CPU、内存或磁盘，必须指定一个 Docker 镜像：

```typescript
const sandbox = await DaytonaSandbox.create({
  image: "node:20",
  language: "typescript",
  resources: {
    cpu: 4,       // CPU 核心数
    memory: 8,    // 内存大小（GiB）
    disk: 50,     // 磁盘大小（GiB）
  },
});
```


## 可用区域

| 区域 | 位置 |
|--------|----------|
| `us` | 美国 |
| `eu` | 欧洲 |

## 访问 Daytona SDK

对于高级功能，可以访问底层的 Daytona SDK：

```typescript
const daytonaSandbox = await DaytonaSandbox.create();
const sdk = daytonaSandbox.sandbox;

// 直接使用任何 Daytona SDK 功能
const workDir = await sdk.getWorkDir();
const homeDir = await sdk.getUserHomeDir();
await sdk.fs.createFolder("src", "755");
```


## 重新连接到现有沙盒

```typescript
// 创建带有自动停止间隔的沙盒
const sandbox = await DaytonaSandbox.create({
  language: "typescript",
  autoStopInterval: 60,
});
const sandboxId = sandbox.id;

// 停止沙盒（保持其可用状态）
await sandbox.stop();

// 稍后：重新连接
const reconnected = await DaytonaSandbox.connect(sandboxId);
await reconnected.start();
const result = await reconnected.execute("ls -la");
```


## 沙盒生命周期

```typescript
const sandbox = await DaytonaSandbox.create();

await sandbox.stop();   // 停止（可以重新启动）
await sandbox.start();  // 启动已停止的沙盒
await sandbox.close();  // 永久删除
```


## 工厂函数

```typescript
import { createDaytonaSandboxFactory, createDaytonaSandboxFactoryFromSandbox } from "@langchain/daytona";

// 每次调用创建新沙盒
const factory = createDaytonaSandboxFactory({ language: "typescript" });

// 或者在多次调用间复用现有沙盒
const sandbox = await DaytonaSandbox.create();
const reuseFactory = createDaytonaSandboxFactoryFromSandbox(sandbox);
```


## 错误处理

```typescript
import { DaytonaSandboxError } from "@langchain/daytona";

try {
  await sandbox.execute("some command");
} catch (error) {
  if (error instanceof DaytonaSandboxError) {
    switch (error.code) {
      case "NOT_INITIALIZED":
        await sandbox.initialize();
        break;
      case "COMMAND_TIMEOUT":
        console.error("命令执行时间过长");
        break;
      case "AUTHENTICATION_FAILED":
        console.error("请检查您的 Daytona API 密钥");
        break;
    }
  }
}
```

### 错误代码

| 代码 | 描述 |
|------|-------------|
| `NOT_INITIALIZED` | 沙盒未初始化 - 调用 `initialize()` |
| `ALREADY_INITIALIZED` | 无法重复初始化 |
| `AUTHENTICATION_FAILED` | Daytona API 密钥无效或缺失 |
| `SANDBOX_CREATION_FAILED` | 创建沙盒失败 |
| `SANDBOX_NOT_FOUND` | 沙盒 ID 未找到或已被删除 |
| `SANDBOX_NOT_STARTED` | 沙盒未处于启动状态 |
| `COMMAND_TIMEOUT` | 命令执行超时 |
| `COMMAND_FAILED` | 命令执行失败 |
| `FILE_OPERATION_FAILED` | 文件读写失败 |
| `RESOURCE_LIMIT_EXCEEDED` | CPU、内存或存储限制超出 |

## 环境变量

| 变量 | 描述 |
|----------|-------------|
| `DAYTONA_API_KEY` | Daytona API 密钥（必需） |
| `DAYTONA_API_URL` | 自定义 Daytona API URL |
| `DAYTONA_TARGET` | 默认目标区域（us/eu） |

---

<div className="source-links">
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/i18n\zh-CN\oss\javascript\integrations\providers\daytona.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
</div>

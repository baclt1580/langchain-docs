---
title: "Premai 集成"
description: "使用 LangChain Python 与 Premai 嵌入模型进行集成。"
---

[PremAI](https://premai.io/) 是一个一体化平台，简化了由生成式 AI 驱动的稳健、生产就绪应用程序的创建。通过简化开发流程，PremAI 使您能够专注于提升用户体验并推动应用程序的整体增长。您可以快速 [开始使用 PremAI](https://docs.premai.io/quick-start)。

### 安装和设置

我们首先安装 `langchain` 和 `premai-sdk`。您可以输入以下命令进行安装：

```bash
pip install premai langchain
```

在继续之前，请确保您已在 PremAI 上创建了账户并已创建项目。如果没有，请参考 [快速入门](https://docs.premai.io/introduction) 指南以开始使用 PremAI 平台。创建您的第一个项目并获取您的 API 密钥。

## PremEmbeddings

在本节中，我们将讨论如何使用 `PremEmbeddings` 配合 LangChain 访问不同的嵌入模型。让我们先导入模块并设置我们的 API 密钥。

```python
# Let's start by doing some imports and define our embedding object

from langchain_community.embeddings import PremAIEmbeddings
```

导入所需模块后，让我们设置客户端。目前假设我们的 `project_id` 是 `8`。但请确保使用您的 project-id，否则将抛出错误。

> 注意：与 [ChatPremAI.](https://python.langchain.com/v0.1/docs/integrations/chat/premai./) 不同，为 PremAIEmbeddings 设置 `model_name` 参数是必需的。

```python
import getpass
import os

if os.environ.get("PREMAI_API_KEY") is None:
    os.environ["PREMAI_API_KEY"] = getpass.getpass("PremAI API Key:")
```

```python
model = "text-embedding-3-large"
embedder = PremAIEmbeddings(project_id=8, model=model)
```

我们支持许多最先进的嵌入模型。您可以查看我们 [支持的 LLM 和嵌入模型列表](https://docs.premai.io/get-started/supported-models)。目前，本示例让我们选择 `text-embedding-3-large` 模型。

```python
query = "Hello, this is a test query"
query_result = embedder.embed_query(query)

# Let's print the first five elements of the query embedding vector

print(query_result[:5])
```

```text
[-0.02129288576543331, 0.0008162345038726926, -0.004556538071483374, 0.02918623760342598, -0.02547479420900345]
```

最后，让我们嵌入一个文档

```python
documents = ["This is document1", "This is document2", "This is document3"]

doc_result = embedder.embed_documents(documents)

# Similar to previous result, let's print the first five element
# of the first document vector

print(doc_result[0][:5])
```

```text
[-0.0030691148713231087, -0.045334383845329285, -0.0161729846149683, 0.04348714277148247, -0.0036920777056366205]
```

---

<div className="source-links">
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/i18n\zh-CN\oss\python\integrations\embeddings\premai.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
</div>

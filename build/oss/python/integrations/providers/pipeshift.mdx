---
title: "Pipeshift 集成"
description: "使用 LangChain Python 与 Pipeshift 集成。"
---

> [Pipeshift](https://pipeshift.com) 是一个用于开源大语言模型的微调与推理平台

- 您提供数据集。微调多个大语言模型。一键启动推理，并见证其扩展至数百万级别。

## 安装与设置

- 安装 Pipeshift 集成包。

  ```bash
  pip install langchain-pipeshift
  ```

- 在 [Pipeshift](https://pipeshift.com) 注册以获取您的 Pipeshift API 密钥。

### 身份验证

您可以使用以下任一方式通过 Pipeshift API 密钥进行身份验证：

1. 将 API 密钥添加到环境变量中，名为 `PIPESHIFT_API_KEY`。

    ```python
    os.environ["PIPESHIFT_API_KEY"] = "<your_api_key>"
    ```

2. 向 pipeshift LLM 模块或聊天模块传递 `api_key`

    ```python
    llm = Pipeshift(api_key="<your_api_key>", model="meta-llama/Meta-Llama-3.1-8B-Instruct", max_tokens=512)

                        OR

    chat = ChatPipeshift(api_key="<your_api_key>", model="meta-llama/Meta-Llama-3.1-8B-Instruct", max_tokens=512)
    ```

## 聊天模型

查看 [示例](/oss/python/integrations/chat/pipeshift)。

```python
from langchain_pipeshift import ChatPipeshift
```
## 大语言模型

查看 [示例](/oss/python/integrations/llms/pipeshift)。

```python
from langchain_pipeshift import Pipeshift
```

---

<div className="source-links">
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/i18n\zh-CN\oss\python\integrations\providers\pipeshift.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
</div>
